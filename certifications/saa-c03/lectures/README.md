# Lectures

Raw lecture slide decks (`.pptx`), one per module, numbered to match the module it pairs with (see `../modules/README.md` once the module list is finalized).

Naming convention: `0N-module-slug.pptx`.

Status: planned — no slides yet.
